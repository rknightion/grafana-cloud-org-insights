name: zizmor

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions: {}

jobs:
  zizmor:
    permissions:
      contents: read
      security-events: write # upload zizmor results
      actions: read          # inspect workflow metadata
    uses: rknightion/.github/.github/workflows/zizmor.yml@0e21ae9045ca3e5ae522b711030b70c53b8d94be # v1.15.0
