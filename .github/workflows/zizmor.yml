name: zizmor

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions: {}

jobs:
  zizmor:
    permissions:
      contents: read
      security-events: write # upload zizmor results
      actions: read          # inspect workflow metadata
    uses: rknightion/.github/.github/workflows/zizmor.yml@0e56520d848f5fda8cbfa0bda17797ef41f2a5a7 # v1.9.8
