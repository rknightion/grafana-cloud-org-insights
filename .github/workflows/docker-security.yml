name: Docker Security

on:
  push:
    branches: [main]
  schedule:
    - cron: "29 5 * * 1"
  workflow_dispatch: {}

permissions: {}

jobs:
  scan:
    permissions:
      contents: read
      security-events: write # upload hadolint and Trivy results
    uses: rknightion/.github/.github/workflows/docker-security.yml@8d14adaa295c4a7670b1d4e744b070955b710598 # v1.24.0
