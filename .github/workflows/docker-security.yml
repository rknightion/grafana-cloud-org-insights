name: Docker Security

on:
  push:
    branches: [main]
  schedule:
    - cron: "29 5 * * 1"
  workflow_dispatch: {}

permissions: {}

jobs:
  scan:
    permissions:
      contents: read
      security-events: write # upload hadolint and Trivy results
    uses: rknightion/.github/.github/workflows/docker-security.yml@e5fb83e8b9fac5d9bcca87c9f189ec3ea946de84 # v1.21.1
