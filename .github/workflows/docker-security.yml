name: Docker Security

on:
  push:
    branches: [main]
  schedule:
    - cron: "29 5 * * 1"
  workflow_dispatch: {}

permissions: {}

jobs:
  scan:
    permissions:
      contents: read
      security-events: write # upload hadolint and Trivy results
    uses: rknightion/.github/.github/workflows/docker-security.yml@27b0bd31dbe7bf238b89773d00d60ed5dfe187bb # v1.20.0
