name: Scorecard

on:
  push:
    branches: [main]
  schedule:
    - cron: "43 5 * * 1"

permissions: {}

jobs:
  scorecard:
    permissions:
      security-events: write # upload Scorecard results
      id-token: write        # publish signed results to OpenSSF
    uses: rknightion/.github/.github/workflows/scorecard.yml@0e21ae9045ca3e5ae522b711030b70c53b8d94be # v1.15.0
