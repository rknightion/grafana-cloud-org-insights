name: Scorecard

on:
  push:
    branches: [main]
  schedule:
    - cron: "43 5 * * 1"

permissions: {}

jobs:
  scorecard:
    permissions:
      security-events: write # upload Scorecard results
      id-token: write        # publish signed results to OpenSSF
    uses: rknightion/.github/.github/workflows/scorecard.yml@e5fb83e8b9fac5d9bcca87c9f189ec3ea946de84 # v1.21.1
