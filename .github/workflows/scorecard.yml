name: Scorecard

on:
  push:
    branches: [main]
  schedule:
    - cron: "43 5 * * 1"

permissions: {}

jobs:
  scorecard:
    permissions:
      security-events: write # upload Scorecard results
      id-token: write        # publish signed results to OpenSSF
    uses: rknightion/.github/.github/workflows/scorecard.yml@c636bd20758433033cb4fe7760867b1318523bbb # v1.17.0
