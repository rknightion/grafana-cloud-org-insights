name: Dependency Review

on:
  pull_request:
    branches: [main]

permissions: {}

jobs:
  dependency-review:
    permissions:
      contents: read
      pull-requests: write # post the dependency review summary
    uses: rknightion/.github/.github/workflows/dependency-review.yml@0e56520d848f5fda8cbfa0bda17797ef41f2a5a7 # v1.9.8
