name: Dependency Review

on:
  pull_request:
    branches: [main]

permissions: {}

jobs:
  dependency-review:
    permissions:
      contents: read
      pull-requests: write # post the dependency review summary
    uses: rknightion/.github/.github/workflows/dependency-review.yml@32b5e93dcbb5419fd955f9eb913720cfbc1c13f8 # v1.25.0
