name: Dependency Review

on:
  pull_request:
    branches: [main]

permissions: {}

jobs:
  dependency-review:
    permissions:
      contents: read
      pull-requests: write # post the dependency review summary
    uses: rknightion/.github/.github/workflows/dependency-review.yml@8c1e89103d1a86179c2e94356b85bedd7c543a42 # v1.9.3
